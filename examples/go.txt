// SPDX-License-Identifier: MIT
pragma solidity ^0.8.0;

contract CounterSafe {
    uint256 public count;

    function increment() external {
        count += 1;
    }

    function decrement() external {
        require(count > 0, "underflow");
        count -= 1;
    }
}